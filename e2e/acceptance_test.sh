#!/usr/bin/env bash

account=$1

echo "account = ${account} run acceptance tests"
echo "this doesnt actually test anything but is a description of the process"
echo "run acceptance tests against deployed micro-service: e.g. behave based tests utilising aws boto3"
echo "run consumer based contract tests against deployed micro-service, e.g. pact"
